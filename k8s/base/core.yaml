apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: core
  template:
    metadata:
      labels:
        app: core
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: core
          image: jagoan-pos/core:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: tcp
              containerPort: 4001
          envFrom:
            - configMapRef:
                name: service-config
            - secretRef:
                name: core-env
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: tcp
            initialDelaySeconds: 10
          livenessProbe:
            tcpSocket:
              port: tcp
            initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: core
spec:
  selector:
    app: core
  ports:
    - name: tcp
      port: 4001
      targetPort: tcp
  type: ClusterIP
